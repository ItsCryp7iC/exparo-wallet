<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.NoActionBar">
        <item name="android:colorPrimary">@color/exparo</item>
        <item name="android:colorPrimaryDark">@color/exparo</item>
        <item name="android:colorAccent">@color/exparo</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:colorControlActivated">@color/exparo</item>
    </style>

</resources>
